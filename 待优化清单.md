# InfinityTime 待优化清单

> 这是针对当前 `InfinityTime`（无限时光）主题 + 插件的后续优化点清单。已做的基础清理（移除死 JS/CSS/标记、未引用资源、`detectTools` 缓存、图集列表合并查询）已在代码中完成；以下为**尚未处理、按优先级排序**的事项。

## P0 · 正确性 / 结构（建议优先）

### 1. 合并 `main.js` 里重复的 poptrox 初始化
- **现状**：`assets/js/main.js` 中出现了**两次** `$main.poptrox({...})`（约第 245 行与第 355 行），会各自生成一套 `.poptrox-overlay / .poptrox-popup`，造成重复的灯箱 DOM。
- **影响**：一个页面有两份弹窗 DOM；此前 EXIF 侧栏正是受此影响（我改用「轮询 + 单数据源」绕开了它，但仍是隐患）。
- **建议**：确认两处职责后合并为一次 `poptrox` 初始化；保留其中 `onPopupClose/onPopupOpen + 移动端横滑` 的能力。**改动较有风险，需回归灯箱打开/关闭/切换。**

### 2. 真正的「图集内多图切换」
- **现状**：主题原蓝图是「一个文章里多张图」用 `.breadcrumb-nav/.nav-dot/.pic-swipe` 在灯箱内切换；但 poptrox 生成的弹窗里并没有这些元素，我已在清理时移除了失效的 `.breadcrumb-nav/.nav-dot` 标记与对应 JS/CSS。因此当前 poptrox 的「上一张/下一张」实际是**跨图集**切换（每个图集封面看作一张），图集内部多图无法直接翻。
- **建议**：若产品需要「一个图集内的多图可左右翻」，需重新接入灯箱内多图切换（poptrox 的 `_poptrox` 集合或自研 swipe）。这属于功能增强，不是简单清理。

## P1 · 性能

### 3. EXIF 侧栏轮询只在灯箱开启时运行
- **现状**：`index.php` 内联 JS 用一个 `setInterval(..., 120)` **常驻轮询**，即使灯箱关闭也在跑。
- **建议**：改为「灯箱打开时启动、关闭时停止」；信号可用「overlay 出现/隐藏」或 `onPopupOpen/onPopupClose`。能省掉背景空转的定时器回调。

> ✅ 已在 v1.3.0 完成：通过 MutationObserver 监听 `.poptrox-overlay` 显隐，开灯箱启动、关灯箱停止，且关闭时恢复背景滚动。

### 4. 图片懒加载改用原生 + IntersectionObserver
- **现状**：主题用 `checkImgs/loadImg/throttle`（滚动监听 + 手工判 `isInSight`）做缩略图懒加载。
- **建议**：给缩略图 `<img>` 加原生 `loading="lazy" decoding="async"`，并可选把滚动监听升级为 `IntersectionObserver`，实现更省、更稳的懒加载。

### 5. 图片体积 / 响应式
- **现状**：`full/` 与 `thumb/` 都是按设置生成 WebP；前端 `full` 直接用于灯箱。
- **建议**：
  - 后台「全图最长边上限」默认 `0`（不裁剪），超大图会原样显示；可结合 `maxWidth` 限制；
  - 给灯箱大图加 `max-width/max-height` 缩放（CSS 已有，可再加 `object-fit: contain` 与占位）；
  - 可选：按设备像素比生成 `@2x` 或 `srcset`；接入 CDN（OSS）图片处理规则缩略图。

## P2 · 代码质量 / 健壮性

### 6. 后台图集其余 N+1
- **现状**：已把 `pp_albums()` 合并为一次查询；但面板里其它地方（如 `rowsFor` 逐图集、`pp_field` 逐字段）仍可能在图集很多时出现较多查询。
- **建议**：图集详情/维护可进一步按 `cid IN (...)` 批量取；仅当这类查询成为瓶颈时再统一。

### 7. EXIF / 相机信息归一化
- **现状**：`lib/ExifReader.php` 解析后字段为 `make/model/iso/fnumber/exposure/focal/datetime` 等。
- **建议**：补充更多 EXIF（如 `lens`、`flash`、`GPS`），并对「时间/快门」等做格式统一；`DateTimeOriginal` 缺失时回退 `gps`/文件时间。

> ✅ v1.3.0：已展示 `lens`、`flash`，焦距统一优先 35mm 等效焦距，时间静态/弹窗格式一致。GPS 坐标展示仍未做（当前以手写地址为准）。

### 8. 文件上传健壮性
- **现状**：`MediaProcessor` 依赖 `php-gd`；HEIC 靠外部工具。上传钩子已有 try/catch 回退。
- **建议**：对超大文件、损坏文件给出更明确的后台错误提示；把「转换失败原因」写入日志并在面板展示；可加「单图最大尺寸」限制。

### 9. 清理/重建的更稳实现
- **现状**：`cleanupOrphans/rebuild` 通过面板 AJAX 分批执行，写 `data/*.json` 进度。
- **建议**：进度文件可加锁/原子写，多个管理员同时操作时避免互踩；完成时对空目录清理与结果统计（已删/已重建/失败）做汇总提示。

## P3 · 体验 / 细节

### 10. 多语言 / 可访问性
- **现状**：`themeConfig` 中文文案为主；个别 `alt`/`aria` 缺失。
- **建议**：补充 `alt` 与 `aria-label`（图标按钮、关闭、上一张/下一张），并用 `lang` 标记；如需双语可加 i18n。

### 11. 版权与说明补充
- **现状**：README 已更新技术栈/功能/体验。
- **建议**：补充「截图/演示」页、`CHANGELOG.md`、主题/插件的 `screenshot`/`info` 成品图；说明 HDR 转 SDR 的局限。

### 12. HDR 局限说明
- **现状**：HEIC 转 WebP 时，WebP 仅支持 8bit SDR，无法保留 10bit/HDR gain map；原始 HEIC 会保留在 `original/` 以备重处理。
- **建议**：在 README/面板注明该限制，避免用户误以为 HDR 被完整保留。

---

## 已完成（供对照）
- 移除灯箱失效的 `mouseover/wheel/nav-next・prev/keydown` 死处理器与 `isTransitioning`；
- 移除卡片失效的 `.breadcrumb-nav/.nav-dot` 标记与对应死 CSS；
- 移除未加载的 `jquery.js`、`jquery.lazyload.js`；
- 移除插件未用的 `Checkbox` import；
- `MediaProcessor::detectTools()` 加静态缓存；
- 后台 `pp_albums()` 由 N+1 改为一次 `IN` 查询。
