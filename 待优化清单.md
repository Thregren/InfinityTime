# InfinityTime 待优化清单

> 这是针对当前 `InfinityTime`（无限时光）主题 + 插件的后续优化点清单。已做的基础清理（移除死 JS/CSS/标记、未引用资源、`detectTools` 缓存、图集列表合并查询）已在代码中完成；以下为**尚未处理、按优先级排序**的事项。

## 2026-09 代码清理 / 健壮性审查（v1.9.0 之后）

### v1.11.0 响应式 / AVIF 与工程化 ✅ 已完成

- 响应式多尺寸：`full` + `@1600` WebP；两档 AVIF（全尺寸 + 1600）。
- 前台灯箱 `srcset` + `sizes` + `<picture>`（AVIF 优先、WebP 回退）。
- schema 版本化迁移（`infinitytimeSchemaVersion`），旧表自动补 `mid / avif / mid_avif`。
- 批量重建同步生成/更新变体，并回写文章字段。
- GitHub Actions CI：PHP / JS 语法、版本一致性、changelog 校验。
- 修复：`variants` 字段写入失败、旧悬停暗角阴影、后台左对齐、PHP 8.5 `imagedestroy`、`Plugin::log` 命名空间。

### v1.11.0 仍未做（下一轮候选）

- 主题内联 JS（EXIF / 全景 / 主题色）外置到 `assets/js/lightbox.js`，可选 esbuild 压缩。
- 后台图集列表按需 AJAX 加载图片（目前缩略图已 lazy，但图集很多时 DOM 仍大）。
- 前台 JSON API 替代 `data-*` 大属性（当前每篇多为单图，收益有限）。
- 主图 `full` / `thumb` 仍走 GD；变体已用 Imagick，主图可再改 Imagick 优先。
- 上传进度目前是整请求字节进度，可改逐张上传显示 X/Y。

### 后台面板改造（v1.10.0）✅ 已完成

- **视觉重做**：现代卡片 + 标签页导航（上传发布 / 已发布图集 / 站点信息 / 联系方式 / 转换设置 / 维护），样式抽到 `assets/admin.css`。
- **逻辑抽离**：后台 JS 抽到 `assets/admin.js` 并收进 IIFE，`panel.php` 从 1496 行降到约 1100 行，页面更小、可缓存。
- **上传体验**：拖拽上传 + 多选、逐图标题/描述、**拖拽排序**、XHR 上传进度、已选总大小提示。
- **图集管理**：缩略图 `loading="lazy"`、**拖拽调整图片顺序**（写 `sort` 字段并同步文章字段）、图集列表 **局部刷新**（`albums_html`，不再整页重渲染）。
- **维护板块**：进度条开始归零、刷新页面后可继续未完成任务、90s 并发锁、GET 维护任务加 CSRF token。
- **P0 修复**：恢复默认设置改走 AJAX、object URL 回收、上传预览内存不再累积。
- **安全加固**：POST/GET 写操作 CSRF token、「关于」HTML 清洗、头像 URL 协议校验、清理非插件文章加事务、上传单文件大小/像素前置校验。
- **性能**：`MediaProcessor` 显式 `imagedestroy`，批量重建不再堆积 GD 位图。

### 后台面板 · 仍可继续

- 图集很多时仍是服务端一次渲染（缩略图已 lazy，但 DOM 仍大）；可改成分页 / 展开时 AJAX 拉图片。
- 删除 / 清理的二次确认仍是原生 `confirm`，可换自定义弹窗。
- 上传进度目前是“整个请求的字节进度”，不是“已完成 X / Y 张”；可改逐张上传。
- `PP_ICONS` 仍是硬编码列表，和 iconfont 实际图标容易脱节。
- `update_album` 的多字段仍是逐条 delete + insert（Typecho 的 `rows()` 不支持多行 insert）。

### ✅ 本轮已修复

- **`main.js`：无限瀑布流翻页后 blur-up / 全景 / 图集数据不重建**。`previewMap / panoMap / ALBUMS` 原本只在 `DOMContentLoaded` 构建一次，`__rebindPoptrox` 只重绑 poptrox；新加载的卡片没有 blur-up 预览、全景识别和图集索引。现抽成 `rebuildLightboxData()`，重绑时同步重建。
- **`main.js`：移除冗余的全页 MutationObserver**（`childList + subtree + attributes style/class`）。`isPopupActive` 已由 `onPopupOpen / onPopupClose` 维护，该观察器在每次 class/style 变化时都会跑回调，属无效开销。
- **`main.js`：`src` 观察器只处理 `<img>`**，其它元素的 src 变化不再进入回调。
- **`main.js`：清理死代码**——`onPopupClose` 里已不存在的 `.pic-swipe-wrapper` 清理、`inAlbumNav` 里的空 `if`；`touchmove` 的 `e.target.closest` 加守卫。
- **CSS：移除失效的 `.pic-swipe-*` 规则**（滑动切图早已移除）。
- **`index.php`：主题色异步回调加 src 校验**，快速切图时旧图的采样结果不会覆盖新图。
- **`ImageRepository::ingest`：上传校验修复**——`tmp_name` 非空但 `is_uploaded_file=false` 时原来会继续处理，现在直接拒绝，避免把任意本地路径当上传图复制进公开目录；顺带移除未使用的 `finfo` 读取。
- **`ImageRepository::toAbs`：路径穿越防御**——先剥离 `..` 段，DB / 字段路径被写脏时也不会读写上传根 / 站点根之外的文件；`unlinkFiles` 对异常做了兜底日志。
- **主题 `main.js` 收敛**：删除 Multiverse 模板中已失效的 4 段代码（header 链接重定向、footer `.copyright` 搬移、`$main.children('.thumb')` 懒加载、`.next-page-btn` hover），以及未使用的 `$wrapper` 变量；约减 100 行。
- **`main.js` 作用域收敛**：自定义逻辑（`ppParseArr` / `fullscreenAPI` / 灯箱交互）包进独立 IIFE，不再往 `window` 上挂多余全局；`window.__rebindPoptrox` 等对外接口仍显式挂载。
- **全屏切换加兼容守卫**：浏览器不支持元素全屏时直接忽略点击，不再抛错。
- **CSS 清理**：删除未匹配任何标记的 `#footer .copyright` 系列规则（实际标记用的是 `.copyright-info`）。

### 🔍 仍待处理（按优先级）

- **P1 性能**：`previewMap / panoMap / ALBUMS` 每次翻页全量重建（O(卡片数)），当前量级可接受；单页数百张可改增量追加。
- **P1 性能**：`analyzePhoto` 每次切图 `new Image()` 全图解码 + 300px canvas 采样，低端机首次有瞬时开销；可降低采样上限，或全景改用缩略图采样。
- **P2**：`panel.php` 进度文件已用 `tmp + rename` 原子写，但多管理员同时操作仍可能互踩（无锁）。
- **P2**：后台写操作目前靠 `HTTP_REFERER` 同源校验防 CSRF，可加 Typecho token / nonce 做双保险。
- **P2**：`ingest` 缺少统一的上传大小 / 像素上限校验（依赖 `post_max_size` 与 GD 内存），可前置检查并给出明确报错。
- **P3**：`main.js` 顶部的 Multiverse 模板代码（panels / breakpoints / is-preload）仍是当前「关于」面板与响应式灯箱边距所依赖的，保留；后续若要彻底去掉模板依赖，需要先自研面板开合与断点监听。

## P0 · 正确性 / 结构（建议优先）

### ⭐ 无限瀑布流新增图片未绑定灯箱（点击直接跳原图）—— ✅ 已解决
- **现状**：poptrox 只在初始化时遍历 `#main a.image` 逐个绑定点击（并移除其 `href`）；主 `ALBUMS`/`previewMap`（灯箱内多图切换 / blur-up 预览）也是在 `DOMContentLoaded` 一次性构建。而「无限瀑布流」翻页用 `fetch` 拉到第 2 页后 `appendChild` 插入新卡片，这些**新卡片从未被 poptrox 处理，也没加入 `ALBUMS/previewMap`**。
- **影响**：滚动加载出来的图片（如「咕咕」「七彩水母」）点击后**直接跳转到 `.webp` 原图**（灯箱不弹）；这些图集的「图集内上一张/下一张/左右滑动」、EXIF 侧栏联动、blur-up 预览也都不生效。
- **✅ 已解决（v1.7.2 / v1.7.4）**：
  1. `loadMore` 翻页后安全**重绑一次 poptrox**：poptrox 只绑定「还有 `href`」的锚点，而早已绑定的卡片 `href` 已被移除，重绑前先从 `data-images[0]` 补回 `href`，再清旧 overlay/popup、解除旧绑定、重新挂 EXIF/全景观察器（灯箱打开时跳过）。
  2. 瀑布流改为 **CSS 列布局**，卡片 DOM 保持**源码顺序**，灯箱「上一张/下一张」天然按源码顺序；新加载卡片同样走 `__rebindPoptrox` 重绑。
- **遗留（低优先级）**：`ALBUMS`/`previewMap` 仍是 `DOMContentLoaded` 一次性构建，重绑后新卡片的 `albumForSrc`/blur 预览可能查不到；但当前每个图集基本=一张图，且顺序已由源码 DOM 保证，影响很小。

### 1. 合并 `main.js` 里重复的 poptrox 初始化（✅ 已解决）
- **现状**：`assets/js/main.js` 中出现了**两次** `$main.poptrox({...})`（约第 245 行与第 355 行），会各自生成一套 `.poptrox-overlay / .poptrox-popup`，造成重复的灯箱 DOM。
- **现状补充**：现已收敛为**单次** `$main.poptrox({...})`，`main.js` 仅一处（约 301 行）。
- **建议**：保留现状；后续动 `main.js` 时回归灯箱开/关/切换即可。

### 2. 真正的「图集内多图切换」
- **现状**：失校的 `.breadcrumb-nav/.nav-dot/.pic-swipe` 死代码已在 v1.8.0 整体移除；当前 poptrox 的「上一张/下一张」= **跨图集**切换（每图集封面看作 1 张），图集内部多图无法直接翻。
- **建议**：若产品需要「一个图集内多图可左右翻」，需重新接入（poptrox 的 `_poptrox` 集合或自研 swipe），或把一张图集拆成多篇。属于功能增强，非必要。

## P1 · 性能

### 3. EXIF 侧栏轮询只在灯箱开启时运行
- **现状**：`index.php` 内联 JS 用一个 `setInterval(..., 120)` **常驻轮询**，即使灯箱关闭也在跑。
- **建议**：改为「灯箱打开时启动、关闭时停止」；信号可用「overlay 出现/隐藏」或 `onPopupOpen/onPopupClose`。能省掉背景空转的定时器回调。

> ✅ 已在 v1.3.0 完成：通过 MutationObserver 监听 `.poptrox-overlay` 显隐，开灯箱启动、关灯箱停止，且关闭时恢复背景滚动。

### 4. 图片懒加载改用原生 + IntersectionObserver
- **现状**：主题用 `checkImgs/loadImg/throttle`（滚动监听 + 手工判 `isInSight`）做缩略图懒加载。
- **建议**：给缩略图 `<img>` 加原生 `loading="lazy" decoding="async"`，并可选把滚动监听升级为 `IntersectionObserver`，实现更省、更稳的懒加载。

### 5. 图片体积 / 响应式
- **现状**：`full/` 与 `thumb/` 都是按设置生成 WebP；前端 `full` 直接用于灯箱。
- **建议**：
  - 后台「全图最长边上限」默认 `0`（不裁剪），超大图会原样显示；可结合 `maxWidth` 限制；
  - 给灯箱大图加 `max-width/max-height` 缩放（CSS 已有，可再加 `object-fit: contain` 与占位）；
  - 可选：按设备像素比生成 `@2x` 或 `srcset`；接入 CDN（OSS）图片处理规则缩略图。

## P2 · 代码质量 / 健壮性

### 6. 后台图集其余 N+1
- **现状**：已把 `pp_albums()` 合并为一次查询；但面板里其它地方（如 `rowsFor` 逐图集、`pp_field` 逐字段）仍可能在图集很多时出现较多查询。
- **建议**：图集详情/维护可进一步按 `cid IN (...)` 批量取；仅当这类查询成为瓶颈时再统一。

### 7. EXIF / 相机信息归一化
- **现状**：`Lib/ExifReader.php` 解析后字段为 `make/model/iso/fnumber/exposure/focal/datetime` 等（Linux 区分目录大小写）。
- **建议**：补充更多 EXIF（如 `lens`、`flash`、`GPS`），并对「时间/快门」等做格式统一；`DateTimeOriginal` 缺失时回退 `gps`/文件时间。

> ✅ v1.3.0：已展示 `lens`、`flash`，焦距统一优先 35mm 等效焦距，时间静态/弹窗格式一致。GPS 坐标展示仍未做（当前以手写地址为准）。

### 8. 文件上传健壮性
- **现状**：`MediaProcessor` 依赖 `php-gd`；HEIC 靠外部工具。上传钩子已有 try/catch 回退。
- **建议**：对超大文件、损坏文件给出更明确的后台错误提示；把「转换失败原因」写入日志并在面板展示；可加「单图最大尺寸」限制。

### 9. 清理/重建的更稳实现
- **现状**：`cleanupOrphans/rebuild` 通过面板 AJAX 分批执行，写 `data/*.json` 进度。
- **建议**：进度文件可加锁/原子写，多个管理员同时操作时避免互踩；完成时对空目录清理与结果统计（已删/已重建/失败）做汇总提示。

## P3 · 体验 / 细节

### 10. 多语言 / 可访问性
- **现状**：`themeConfig` 中文文案为主；个别 `alt`/`aria` 缺失。
- **建议**：补充 `alt` 与 `aria-label`（图标按钮、关闭、上一张/下一张），并用 `lang` 标记；如需双语可加 i18n。

### 11. 版权与说明补充
- **现状**：README 已更新技术栈/功能/体验。
- **建议**：补充「截图/演示」页、`CHANGELOG.md`、主题/插件的 `screenshot`/`info` 成品图；说明 HDR 转 SDR 的局限。

### 12. HDR 局限说明
- **现状**：HEIC 转 WebP 时，WebP 仅支持 8bit SDR，无法保留 10bit/HDR gain map；原始 HEIC 会保留在 `original/` 以备重处理。
- **建议**：在 README/面板注明该限制，避免用户误以为 HDR 被完整保留。

---

## JS 审查补充（2026-09）

### A. `main.js` 多处 `JSON.parse` 未加 try/catch（✅ 已修复）
- **涉及**：`assets/js/main.js` 约 363-364（`previewMap`）、414-419（`ALBUMS`）、531 / 605 / 652（`nav.dataset.images`）。
- **影响**：任一图集的 `data-images / data-previews / data-exif / data-titles / data-descs / data-addresses` 为非法 JSON（如旧库脏数据、被手动改坏）时，`document.querySelectorAll('#main a.image').forEach(...)` 会抛异常，**整站的 blur-up 预览 / 灯箱内多图切换静默失效**。
- **✅ 已修复（v1.7.2+）**：`main.js` 全量改用 `ppParseArr()`（非法/缺失回退空数组）。

### B. `main.js` 仍残留失效的「图集内滑动切换」代码（✅ 已修复）
- **涉及**：`ensureSwipeStructure` / `breadcrumb-nav` / `nav-dot` / `pic-swipe-track` 相关（`onPopupOpen` 里有 `ensureSwipeStructure` 调用；`applyLqip` 处 `pic-swipe-wrapper` 等）。
- **影响**：poptrox 生成的真实弹窗里**没有** `.breadcrumb-nav`，这些分支大多 `if (!nav) return` 提前退出，等于没执行；但代码仍在、且 `typeof checkImgs`/`JSON.parse(nav.dataset.images)` 一旦在异常路径被走到就会报错。
- **✅ 已修复（v1.8.0）**：删除整段死代码（`ensureSwipeStructure`/`updateSwipePosition`/`endSwipeDrag`/`getImageSuffix` 等），保留移动端“滑动→上一张/下一张 + 图集内切换”兜底与灯箱内禁止页面滚动。

### C. 两个长期 live 的 `MutationObserver` + document 级捕获监听（部分已完成）
- **涉及**：`main.js` 里对 `document.body` 的一处 `attributeFilter:['src']` 观察（会在每次懒加载换 src 时触发）、一处 `childList+subtree+attributes`（style/class）观察；以及 `ppBindPanoGuard()` 在「首次打开过全景」后**永久**在 `document` 上挂 `pointerdown/pointermove/mousemove/touchmove/click` 捕获监听。
- **影响**：灯箱关闭后这些监听仍存在，滚动/移动鼠标时会多跑几个几乎必返空的回调；心智负担与极少量性能损耗，不是功能 bug。
- **✅ 已部分处理**：`ppUnbindPanoGuard` 已在全景销毁时解绑 document 级捕获监听（v1.7.2）。
- **遗留**：`main.js` 对 `document.body` 的两个 `MutationObserver`（`src` 属性、`style/class`）仍是常驻，且 `src` 观察会在每次懒加载换 src 时触发。可考虑合并或用 `onPopupOpen/Close` 替代，属轻微优化。

### D. EXIF/全景 120ms 轮询回调无顶层 try/catch（✅ 已修复）
- **涉及**：`index.php` 内联 `setInterval(...,120)` 每次调 `syncDockExif()/syncPano()`。
- **影响**：若其中任一函数抛未捕获异常（多依赖其它 `try/catch` 兜底，但非全部），会在控制台**每 120ms** 刷一条报错，且该次侧栏/全景更新停摆。
- **✅ 已修复（v1.7.2+）**：轮询回调整体包 `try/catch`。

---

## 近期新增功能（1.7.4 / 1.8.0）后续待跟进

### X1. 主题色/直方图采样性能（低，可优化）
- **现状**：`analyzePhoto` 在灯箱每次切图时 `new Image()` 全图 + 300px canvas 采样。对 8192×4096 全景会解码整图（通常已缓存），但低端机/首次可能有瞬时开销。
- **建议**：可改用更小的采样上限（如 224px）或用 `createImageBitmap(resize)`；或改为“仅对非全景用全图、全景用缩略图”。采样结果可加缓存（按 src 记忆），避免同一图反复采样。

### X2. 全景缩略图裁切比例可配置（低）
- **现状**：全景缩略图目前固定「中央 4:3」裁切，写死在 `MediaProcessor`。
- **建议**：可加到后台设置（如「全景缩略图比例：4:3 / 16:9 / 1:1」），并纳入重建。

### X3. 移动端 Safari 全屏（已按平台限制处理）
- **现状**：iOS Safari 不支持元素级 `requestFullscreen`，已在**不支持全屏的设备上隐藏全屏按钮**（避免死按钮）。Pannellum 在桌面/Android 正常全屏。
- **建议**：无需改动；如后续 iOS 支持元素全屏，可再放开。可在此记录限制，避免误认为 bug。

### X4. 灯箱氛围/主题色的延伸（可选）
- **现状**：主题色只用于「主题色」+ 直方图展示。
- **建议**：后续可考虑用主色做「灯箱四周氛围光晕」或「卡片主色标题」，属增强。

---

## 已完成（供对照）
- ✅ **v1.7.4**：主题色提取（3 色/大写/真实像素/色相分桶）、全景缩略图 4:3 中央裁切、CSS 列瀑布流（灯箱按源码顺序 + 原生比例丝滑变形）、移动端 Safari 隐藏元素全屏与 Pannellum 自带按钮。
- ✅ **v1.8.0**：灯箱「主题色」下新增 **RGB 直方图**（清晰、与主题色同采样）；移除失效的图集内滑动结构 JS/CSS 死代码；`analyzePhoto`/`drawHistogram` 加空值防护。
- ✅ **2026-09（v1.7.1+）**：
  - 无限瀑布流翻页后安全重绑 poptrox（先补回被移除的 `href` 再重绑，避免“旧卡片丢失绑定”）。
  - `main.js` 全量 `JSON.parse` 改用 `ppParseArr()`（非法/缺失回退空数组），不再因一个脏 `data-*` 弄瘫全站。
  - 灯箱/全景 120ms 轮询回调整体包 `try/catch`，避免单次异常每 120ms 刷控制台。
  - 全景销毁时解绑 document 级捕获监听（`ppUnbindPanoGuard`），不再常驻空转。
  - 全景全屏时**不响应“点击退出/关闭”**（非全屏仍可点击遮罩关闭）；全屏/退出按钮与 Pannellum 控件照常可点。
- 移除灯箱失效的 `mouseover/wheel/nav-next・prev/keydown` 死处理器与 `isTransitioning`；
- 移除卡片失效的 `.breadcrumb-nav/.nav-dot` 标记与对应死 CSS；
- 移除未加载的 `jquery.js`、`jquery.lazyload.js`；
- 移除插件未用的 `Checkbox` import；
- `MediaProcessor::detectTools()` 加静态缓存；
- 后台 `pp_albums()` 由 N+1 改为一次 `IN` 查询。
