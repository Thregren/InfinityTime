# InfinityTime 待优化清单

> 这是针对当前 `InfinityTime`（无限时光）主题 + 插件的后续优化点清单。已做的基础清理（移除死 JS/CSS/标记、未引用资源、`detectTools` 缓存、图集列表合并查询）已在代码中完成；以下为**尚未处理、按优先级排序**的事项。

## P0 · 正确性 / 结构（建议优先）

### ⭐ 无限瀑布流新增图片未绑定灯箱（点击直接跳原图）
- **现状**：poptrox 只在初始化时遍历 `#main a.image` 逐个绑定点击（并移除其 `href`）；主 `ALBUMS`/`previewMap`（灯箱内多图切换 / blur-up 预览）也是在 `DOMContentLoaded` 一次性构建。而「无限瀑布流」翻页用 `fetch` 拉到第 2 页后 `appendChild` 插入新卡片，这些**新卡片从未被 poptrox 处理，也没加入 `ALBUMS/previewMap`**。
- **影响**：滚动加载出来的图片（如「咕咕」「七彩水母」）点击后**直接跳转到 `.webp` 原图**（灯箱不弹）；这些图集的「图集内上一张/下一张/左右滑动」、EXIF 侧栏联动、blur-up 预览也都不生效。
- **✅ 已修复（v1.7.1+）**：在 `loadMore` 翻页后安全地**重绑一次 poptrox**。关键修复：poptrox 只绑定「还有 `href`」的锚点，而早已绑定的卡片 `href` 已被移除，重绑必须先**从 `data-images[0]` 把 `href` 补回**，否则重绑会跳过旧卡片导致「所有灯箱打不开」（此前回退正是咬在此处）。重绑前会清掉旧 overlay/popup、解除旧绑定，重绑后重新挂 EXIF/全景观察器；灯箱打开时跳过重绑。
- **遗留**：`ALBUMS/previewMap` 仍是 `DOMContentLoaded` 一次性构建，重绑后新卡片虽能开灯箱，但其「图集内第 2+ 张」的 `albumForSrc`/滑动切换仍查不到（因为 poptrox 之前没有把多图并进同一条目）。若产品一定需要「一个图集内多图在灯箱内左右翻」，需另做（见第 3 条），否则每个图集封面=一张图即可。

### 1. 合并 `main.js` 里重复的 poptrox 初始化（✅ 已解决）
- **现状**：`assets/js/main.js` 中出现了**两次** `$main.poptrox({...})`（约第 245 行与第 355 行），会各自生成一套 `.poptrox-overlay / .poptrox-popup`，造成重复的灯箱 DOM。
- **现状补充**：现已收敛为**单次** `$main.poptrox({...})`，`main.js` 仅一处（约 301 行）。
- **建议**：保留现状；后续动 `main.js` 时回归灯箱开/关/切换即可。

### 2. 真正的「图集内多图切换」
- **现状**：主题原蓝图是「一个文章里多张图」用 `.breadcrumb-nav/.nav-dot/.pic-swipe` 在灯箱内切换；但 poptrox 生成的弹窗里并没有这些元素，我已在清理时移除了失效的 `.breadcrumb-nav/.nav-dot` 标记与对应 JS/CSS。因此当前 poptrox 的「上一张/下一张」实际是**跨图集**切换（每个图集封面看作一张），图集内部多图无法直接翻。
- **建议**：若产品需要「一个图集内的多图可左右翻」，需重新接入灯箱内多图切换（poptrox 的 `_poptrox` 集合或自研 swipe）。这属于功能增强，不是简单清理。

## P1 · 性能

### 3. EXIF 侧栏轮询只在灯箱开启时运行
- **现状**：`index.php` 内联 JS 用一个 `setInterval(..., 120)` **常驻轮询**，即使灯箱关闭也在跑。
- **建议**：改为「灯箱打开时启动、关闭时停止」；信号可用「overlay 出现/隐藏」或 `onPopupOpen/onPopupClose`。能省掉背景空转的定时器回调。

> ✅ 已在 v1.3.0 完成：通过 MutationObserver 监听 `.poptrox-overlay` 显隐，开灯箱启动、关灯箱停止，且关闭时恢复背景滚动。

### 4. 图片懒加载改用原生 + IntersectionObserver
- **现状**：主题用 `checkImgs/loadImg/throttle`（滚动监听 + 手工判 `isInSight`）做缩略图懒加载。
- **建议**：给缩略图 `<img>` 加原生 `loading="lazy" decoding="async"`，并可选把滚动监听升级为 `IntersectionObserver`，实现更省、更稳的懒加载。

### 5. 图片体积 / 响应式
- **现状**：`full/` 与 `thumb/` 都是按设置生成 WebP；前端 `full` 直接用于灯箱。
- **建议**：
  - 后台「全图最长边上限」默认 `0`（不裁剪），超大图会原样显示；可结合 `maxWidth` 限制；
  - 给灯箱大图加 `max-width/max-height` 缩放（CSS 已有，可再加 `object-fit: contain` 与占位）；
  - 可选：按设备像素比生成 `@2x` 或 `srcset`；接入 CDN（OSS）图片处理规则缩略图。

## P2 · 代码质量 / 健壮性

### 6. 后台图集其余 N+1
- **现状**：已把 `pp_albums()` 合并为一次查询；但面板里其它地方（如 `rowsFor` 逐图集、`pp_field` 逐字段）仍可能在图集很多时出现较多查询。
- **建议**：图集详情/维护可进一步按 `cid IN (...)` 批量取；仅当这类查询成为瓶颈时再统一。

### 7. EXIF / 相机信息归一化
- **现状**：`Lib/ExifReader.php` 解析后字段为 `make/model/iso/fnumber/exposure/focal/datetime` 等（Linux 区分目录大小写）。
- **建议**：补充更多 EXIF（如 `lens`、`flash`、`GPS`），并对「时间/快门」等做格式统一；`DateTimeOriginal` 缺失时回退 `gps`/文件时间。

> ✅ v1.3.0：已展示 `lens`、`flash`，焦距统一优先 35mm 等效焦距，时间静态/弹窗格式一致。GPS 坐标展示仍未做（当前以手写地址为准）。

### 8. 文件上传健壮性
- **现状**：`MediaProcessor` 依赖 `php-gd`；HEIC 靠外部工具。上传钩子已有 try/catch 回退。
- **建议**：对超大文件、损坏文件给出更明确的后台错误提示；把「转换失败原因」写入日志并在面板展示；可加「单图最大尺寸」限制。

### 9. 清理/重建的更稳实现
- **现状**：`cleanupOrphans/rebuild` 通过面板 AJAX 分批执行，写 `data/*.json` 进度。
- **建议**：进度文件可加锁/原子写，多个管理员同时操作时避免互踩；完成时对空目录清理与结果统计（已删/已重建/失败）做汇总提示。

## P3 · 体验 / 细节

### 10. 多语言 / 可访问性
- **现状**：`themeConfig` 中文文案为主；个别 `alt`/`aria` 缺失。
- **建议**：补充 `alt` 与 `aria-label`（图标按钮、关闭、上一张/下一张），并用 `lang` 标记；如需双语可加 i18n。

### 11. 版权与说明补充
- **现状**：README 已更新技术栈/功能/体验。
- **建议**：补充「截图/演示」页、`CHANGELOG.md`、主题/插件的 `screenshot`/`info` 成品图；说明 HDR 转 SDR 的局限。

### 12. HDR 局限说明
- **现状**：HEIC 转 WebP 时，WebP 仅支持 8bit SDR，无法保留 10bit/HDR gain map；原始 HEIC 会保留在 `original/` 以备重处理。
- **建议**：在 README/面板注明该限制，避免用户误以为 HDR 被完整保留。

---

## JS 审查补充（2026-09）

### A. `main.js` 多处 `JSON.parse` 未加 try/catch（健壮性）
- **涉及**：`assets/js/main.js` 约 363-364（`previewMap`）、414-419（`ALBUMS`）、531 / 605 / 652（`nav.dataset.images`）。
- **影响**：任一图集的 `data-images / data-previews / data-exif / data-titles / data-descs / data-addresses` 为非法 JSON（如旧库脏数据、被手动改坏）时，`document.querySelectorAll('#main a.image').forEach(...)` 会抛异常，**整站的 blur-up 预览 / 灯箱内多图切换静默失效**。
- **建议**：用 `try { JSON.parse(...) } catch { [] }` 包一层（`index.php` 内联的同名 `articleData` 已这么做）；即便 `data-*` 缺失也回退为空数组。

### B. `main.js` 仍残留失效的「图集内滑动切换」代码（死代码）
- **涉及**：`ensureSwipeStructure` / `breadcrumb-nav` / `nav-dot` / `pic-swipe-track` 相关（`onPopupOpen` 里有 `ensureSwipeStructure` 调用；`applyLqip` 处 `pic-swipe-wrapper` 等）。
- **影响**：poptrox 生成的真实弹窗里**没有** `.breadcrumb-nav`，这些分支大多 `if (!nav) return` 提前退出，等于没执行；但代码仍在、且 `typeof checkImgs`/`JSON.parse(nav.dataset.images)` 一旦在异常路径被走到就会报错。
- **建议**：若确定不再走「图集内 swipe」，可整体删除；否则应按「事件委托 + 懒构建」重做（见 P0 ⭐）。

### C. 两个长期 live 的 `MutationObserver` + document 级捕获监听（轻微开销）
- **涉及**：`main.js` 里对 `document.body` 的一处 `attributeFilter:['src']` 观察（会在每次懒加载换 src 时触发）、一处 `childList+subtree+attributes`（style/class）观察；以及 `ppBindPanoGuard()` 在「首次打开过全景」后**永久**在 `document` 上挂 `pointerdown/pointermove/mousemove/touchmove/click` 捕获监听。
- **影响**：灯箱关闭后这些监听仍存在，滚动/移动鼠标时会多跑几个几乎必返空的回调；心智负担与极少量性能损耗，不是功能 bug。
- **建议**：可让 guard 在灯箱关闭/全景销毁后 `removeEventListener`（或仅在 `.pp-pano-viewer` 存在时启用）；两个 body Observer 视需要合并或改用事件委托。

### D. EXIF/全景 120ms 轮询回调无顶层 try/catch（排障噪音）
- **涉及**：`index.php` 内联 `setInterval(...,120)` 每次调 `syncDockExif()/syncPano()`。
- **影响**：若其中任一函数抛未捕获异常（多依赖其它 `try/catch` 兜底，但非全部），会在控制台**每 120ms** 刷一条报错，且该次侧栏/全景更新停摆。
- **建议**：轮询回调整体包一层 `try {} catch (e) {}`，至少避免持续刷屏；正常路径无影响。

---

## 已完成（供对照）
- ✅ **2026-09（v1.7.1+）**：
  - 无限瀑布流翻页后安全重绑 poptrox（先补回被移除的 `href` 再重绑，避免“旧卡片丢失绑定”）。
  - `main.js` 全量 `JSON.parse` 改用 `ppParseArr()`（非法/缺失回退空数组），不再因一个脏 `data-*` 弄瘫全站。
  - 灯箱/全景 120ms 轮询回调整体包 `try/catch`，避免单次异常每 120ms 刷控制台。
  - 全景销毁时解绑 document 级捕获监听（`ppUnbindPanoGuard`），不再常驻空转。
  - 全景全屏时**不响应“点击退出/关闭”**（非全屏仍可点击遮罩关闭）；全屏/退出按钮与 Pannellum 控件照常可点。
- 移除灯箱失效的 `mouseover/wheel/nav-next・prev/keydown` 死处理器与 `isTransitioning`；
- 移除卡片失效的 `.breadcrumb-nav/.nav-dot` 标记与对应死 CSS；
- 移除未加载的 `jquery.js`、`jquery.lazyload.js`；
- 移除插件未用的 `Checkbox` import；
- `MediaProcessor::detectTools()` 加静态缓存；
- 后台 `pp_albums()` 由 N+1 改为一次 `IN` 查询。
