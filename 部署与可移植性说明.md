# InfinityTime 跨站点部署说明

在**另一台 / 另一个 Typecho 实例**上复现相同的功能与效果，只需安装下面两部分，无需修改任何 Typecho 原生文件或配置文件。

## 一、安装内容

```text
usr/plugins/InfinityTime/     # 插件（含 lib/、panel.php、Plugin.php）
usr/themes/InfinityTime/      # 主题（含 index.php、functions.php、assets/）
```

> 不要拷贝本机 `config.inc.php`（站点数据库/路径配置属于每个站点）；也不要拷贝测试图片、`usr/uploads`、插件 `data/` 下的运行期 `*.json`（自动生成，可忽略）。

## 二、环境要求

- PHP ≥ 7.4（推荐 8.x），需启用 `php-gd`（WebP 与缩略图）、`php-exif`（读取 EXIF）。
- 上传限制：建议把 PHP 的 `upload_max_filesize`、`post_max_size` 设到 **≥ 20M**（手机照片/HEIC 常在 2–8MB，默认 2M 会导致上传失败；超限时后台会提示「图片过大，超过服务器上传限制」）。
- **HEIC/HEIF 支持**（可选）：服务器装 `ImageMagick`（带 libheif）或 `heif-convert`，保证能在 PATH 找到；
  若装在非标准路径，在 **控制台 → 设置 → 插件 → InfinityTime** 里填「heif-convert 路径」即可，无需改代码。
- 数据库：Typecho 支持的 MySQL / SQLite / PostgreSQL 均可。

## 三、部署步骤

1. 上传 `usr/plugins/InfinityTime/` 与 `usr/themes/InfinityTime/`。
2. 后台 → 设置 → 插件，启用 **InfinityTime**（自建 `typecho_infinitytime_images` 表与「InfinityTime」后台菜单）。
3. 后台 → 外观 → 主题，启用 **InfinityTime**。
4. 进入「InfinityTime」菜单：
   - 「上传并发布图集」上传图片，自动转 WebP + 缩略图 + EXIF；
   - 展开图集后，可对每张图片填「标题 / 描述 / 拍摄地址」（可选）；
   - 「WebP 转换设置」用滑块调质量，并可用「恢复默认最佳设置」一键恢复；
   - 「站点信息 / 关于」「联系方式 / 联系我」配置前台页脚与底部栏。
5. 回首页即可看到图片网格 + 灯箱（右侧 EXIF 侧栏）。

## 四、说明

- 前端「关于」、左下角头像/名称/说明、联系方式：优先读插件 `options` 表里的 `infinitytimeSiteName / infinitytimeSiteLogo / infinitytimeSiteTagline / infinitytimeAbout / infinitytimeContacts`，留空自动回退主题自带设置。
- 上传目录跟随 Typecho 的 `uploadDir`，默认 `usr/uploads/`，产物目录 `original/ | full/ | thumb/`。
- HEIC 解码优先级：Imagick → magick/convert（ImageMagick）→ heif-convert → 插件设置里的手动路径。
- 图标已内嵌主题本地（`assets/fonts/` + `assets/css/iconfont.css`），不依赖 CDN。
- 插件 `data/` 存放重建/清理临时进度，已 `.gitignore`。

## 五、维护功能

- 「重建缩略图/全图」：按当前质量/尺寸用原图重生成 `full/`、`thumb/`（有进度、批量）。
- 「清理孤儿文件」：删除不被任何图集引用的 `original/full/thumb`，并清理空目录。
- 图集默认折叠，点击「展开」可编辑信息、单图标题/描述/地址、删除等。
